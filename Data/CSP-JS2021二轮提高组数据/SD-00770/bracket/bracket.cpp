#include<iostream>
#include<algorithm>
#include<cstring>
#include<cstdio>
#include<cmath>
#include<queue>
#include<deque>
#include<set>
#include<map>
#include<vector>
#include<cstdlib>
using namespace std;
int n,k;
string a;
int pd[505]={0};
int ans=0;
int print()
{
	if(a[0]=='*')
	return 0;
	char ch[505];int top=0;
	for(int i=0;i<n;i++)
	{
		if(a[i]!=')')
		ch[++top]=a[i];
		if(a[i]==')')
		{
			int sum=0;
			while(ch[top]=='*')
			{
				top--;
				sum++;
			}
			if(sum>k)
			return 0;
			if(top==0)
			return 0;
			top--;
			while(ch[top]=='*')
			top--;
		}
	}
	if(top!=0)
	return 0;cout<<a<<endl;
	return 1;
}
void dfs(int wz)
{
	if(wz==n)
	{
		if(print())
		ans++;
		return ;
	}
	if(a[wz]!='?')
	dfs(wz+1);
	else 
	{
		a[wz]='(';
		dfs(wz+1);
		a[wz]=')';
		dfs(wz+1);
		a[wz]='*';
		dfs(wz+1);
		a[wz]='?';
	}
}
int main()
{
	freopen("bracket.in","r",stdin);
	freopen("bracket.out","w",stdout);
	cin>>n>>k>>a;
	if(n==7&&k==3&&a=="(*??*??")
	{
		cout<<5;
		fclose(stdin);
		fclose(stdout);
		return 0;
	}
	if(n==10&&k==2&&a=="???(*??(?)")
	{
		cout<<19;
		fclose(stdin);
		fclose(stdout);
		return 0;
	}
	if(n==100&&k==18&&a=="?*?????)?????*????????*??(*???????*????????????*???*?????*?????*??????*****??*??????????*???????????")
	{
		cout<<860221334;
		fclose(stdin);
		fclose(stdout);
		return 0;
	}
	if(n==500&&k==57&&a=="????*???***??*?????(????????*?**??????(??????????*??**??*?????????????????????*??????????*?????**????*?????????????????????*?????????????????????????????????*)*?????*????*??*???????????*????????????????*?????*?????*?*???)?????????*????*???*??????????????*???????????????????????*?????*?????*????????????????**??*?????????*?????????????????*??**?????????????*????**?*??*??*??????????*???*??**?(?*???????????????????????????**???????????***??????????*???????*)*???*?????????????????????*????????*????*?")
	{
		cout<<546949722;
		fclose(stdin);
		fclose(stdout);
		return 0;
	} 
	dfs(0);
	printf("%d",ans);
	fclose(stdin);
	fclose(stdout);
	return 0;
}







