#include<iostream>
#define ll long long

using namespace std;

ll n, m, t;

int main()
{
	freopen("traffic.in", "r", stdin);
	freopen("traffic.out", "w", stdout);
	
	cin >> n >> m >> t;
	
	for(ll i = 1; i <= n - 1; i++)
	{
		for(ll j = 1; j <= m; j++)
		{
			cin >> 
		}
	}
	
	fclose(stdin);
	fclose(stdout);
	
	return 0;
}

