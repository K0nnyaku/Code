#include<iostream>
#include<cstdio>
#include<cstring>
#include<cmath>
#include<algorithm> 
using namespace std;
int main()
int n,m,T,x,y{
scanf("x"x=0,x>=0,x++);
scanf("y"y>0,y>=0,y++);
for(x,y,n-1);
printf("%d/n"(T,i));
if(T>=i);
else(x<=0);
return 0;
}
