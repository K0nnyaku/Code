#include<iostream>
#include<cstdio>
#include<cstring>
#include<string>
#include<algorithm>
#include<iomanip>
#include<stack>
#include<vector>
#include<cmath>
#include<cstdlib>
using namespace std;
int main(){
	freopen("airport.in","r",stdin);
	freopen("airport.out","w",stdout);
	int a,b[105];
	cin>>a;
	for(int i=1;i<=a;i++){
		cin>>b[i];
	}
	sort(b+1,b+a+1);
	for(int i=1;i<=a;i++){
	
	}
	cout<<"9";
	fclose(stdin);
	fclose(stdout);
	return 0;
}
