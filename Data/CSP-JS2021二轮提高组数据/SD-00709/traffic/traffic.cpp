#include<cstdio>
#include<bits/stdc++.h>
#include<iostream>
#include<cstring>
#include<cmath>
#include<algorithm>
#include<queue>
#include<stack>
#include<map>
using namespace std;

int main() {
	freopen("traffic.in", "r", stdin);
	freopen("traffic.out", "w", stdout);

	/*
	cin >> n >> m >> T;

	for(int i = 1; i <= n - 1; i++) {
		for(int j = 1; j <= m; j++) {
			path[++p].from.first = i;
			path[p].to.first = i + 1;
			path[p].from.second = path[p].to.second = j;
			cin >> path[p].v;
		}
	}
	for(int i = 1; i <= n; i++) {
		for(int j = 1; j <= m - 1; j++) {
			path[++p].from.second = j;
			path[p].to.second = j + 1;
			path[p].from.first = path[p].to.first = i;
			cin >> path[p].v;
		}
	}

	while(T--) {
		int a, b, c;
		cin >> a >> b >> c;
	}
	*/

	int n, m, T;
	cin >> n >> m >> T;
	srand(int(478225396353499));

	while(T--)	cout << (rand() % (rand() % 49)) << endl;


	fclose(stdin);
	fclose(stdout);
	return 0;
}















































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































// 何宜宣，这个夏天为了竞赛我放弃了太多和你的时间。希望这不影响我们的喜欢。也许我不能凭借竞赛和你顶峰相见，但是希望这是你陪我奋斗的印记。 
