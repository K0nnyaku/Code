#include<iostream>
#include<cstring>
#include<cstdio>
using namespace std;
char a[500]
int main()
{
	int n,k;
	scanf("%d%d",&n,&k);
	scanf("%s",a);
	for(int i=0;i<=n-1;i++)
	{
		if (a[i]='?')
		{
			
		}
		if (a[i]='*')
		if (a[i]='(')
		if (a[i]=')')
	}
	
	
	
	return 0;
}

